# 进行中需求

> 本目录承载所有 `in-progress` 的需求工作目录。每个需求一个子目录：`docs/specs/active/{feature}/`。
>
> 标准产物：`proposal.md` / `spec.md` / `design.md` / `tasks.md` / `verification.md` / `findings/`。
>
> 通过 `/openharness:propose <feature>` 自动初始化。
