# 接口文档

> 状态：占位骨架，待 `scan-api` 或人工填充
> 模式：greenfield | 更新时间：2026-06-18

## 维护方式

- 每个对外暴露的接口集合在本目录下生成一个独立 `.md` 文件
- 文件内容包含：接口路径、入参 / 出参、错误码、幂等键、调用方
- HTTP REST / Thrift RPC / gRPC 各自归档，文件命名加协议前缀

## 当前状态

仓库尚无对外接口。在首批接口上线后，运行 `scan-api` 自动生成清单。
