# 领域模型

> 状态：占位骨架，待 `scan-service` 或人工填充
> 服务：AgentCron | 模式：greenfield | 更新时间：2026-06-18

## 待补充结构

- 服务定位与核心能力
- 模块详情（每个模块的职责、关键类/函数）
- 数据归属（哪些表由本服务主写，哪些只读）
- 事件流（发布的事件、消费的事件）
- 状态机（核心实体的状态迁移）

## 触发更新时机

- 新增核心业务实体时
- 新增/修改业务流程或状态机时
- 跨服务事件契约变更时

> 维护方式：在业务代码就位后，运行 `scan-service` 重建本文件。
