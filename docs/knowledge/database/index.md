# 数据库文档

> 状态：占位骨架，待 `scan-db` 或人工填充
> 模式：greenfield | 更新时间：2026-06-18

## 维护方式

- 每张业务表在本目录下生成一个独立 `.md` 文件，文件名 = 表名
- 文件内容包含：建表语句、字段说明、索引、关联关系、ER 图节点
- 出现表结构变更（建表、加字段、加索引）时同步更新本目录

## 当前状态

仓库尚无业务表。在首张业务表上线后，运行 `scan-db` 自动生成对应文档。
